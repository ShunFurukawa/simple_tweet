
%p 教えてもらいながら、作成しています。

%h1 こんにちは!! これは、Simple Tweet です。

%div
  -if user_signed_in?
    = link_to "フォロー 一覧 ", relation_path(current_user.id)
    %br
    = link_to "フォロワー 一覧", 'relationed/:id'
    %br
    = link_to "Simple Tweet ユーザー 一覧", users_path
  -else
    %p 使いたいなら、ログインしなさいな。
    %p simple tweetのユーザー一覧を見れたり、つぶやいたり、フォローしたりできまする。
