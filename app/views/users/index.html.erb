<h3>Simple Tweet ユーザー 一覧 です</h3>

<% @users.each do |user|%>

  <table border= "1" width= "50%">
    <tr>
      <td>ID number</td>
      <td>user info</td>
      <td>more...</td>
    </tr>

    <tr>
      <td>囚人番号 <%= user.id %></td>
      <td><%= user.email %>さん</td>
      <td><%= link_to "詳しく", user_path(user.id) %></td>
    </tr>

  </table>

<% end %>

<%= link_to "フォロー 一覧 ", relation_path(current_user.id) %>
<br>
<%= link_to "戻るで", :back %>
