%p
  ユーザー :
  \#{@tweet.user_id}
%p
  タイトル :
  \#{@tweet.title}
%p
  つぶやき内容 :
  \#{@tweet.text}
%div{id: "like-buttons"}
  = render partial: 'likes/like', locals: { tweet: @tweet, like: @like }
%p= link_to "編集する", edit_tweet_path(params[:id])
%p= link_to "削除する", tweet_path, method: :delete, data: { confirm: '削除しますか？' }
%p= link_to "一覧に戻るで", tweets_path
